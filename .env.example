# PostgreSQL
POSTGRES_USER=stonehearts
POSTGRES_PASSWORD=stonehearts
POSTGRES_DB=stonehearts

# Backend
SECRET_KEY=changeme-in-production
BACKEND_CORS_ORIGINS=http://localhost:3000

# Google OAuth (optional — app works without these, but login will be disabled)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# KataGo analysis server (run katago-server separately)
KATAGO_SERVER_URL=ws://host.docker.internal:8001/ws/analyze

# Frontend
API_BASE_URL=http://localhost:8000/api/v1
